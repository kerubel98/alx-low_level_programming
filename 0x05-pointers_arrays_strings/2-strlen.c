#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - takes sting
 * @s - pointes to sring
 * Return : length of string
 */

int _strlen(char *s)
{
	int lngth;
	s = "my first strlen!";


	lngth = strlen(s);

	return (lngth);
}
