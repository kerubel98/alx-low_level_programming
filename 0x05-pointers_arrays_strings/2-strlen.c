#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - takes sting
 * @s - pointes to sring
 * Return : length of string
 */

int _strlen(char *s)
{
	int lngth;


	lngth = strlen(const char *s);

	return (lngth);
}
