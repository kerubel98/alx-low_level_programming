#include "main.h"
#include <stdio.h>

/**
 * _strlen - takes sting
 * @s - pointes to sring
 * Return : length of string
 */

int _strlen(char *s)
{
	char lngth:

	lngth = strlein(*s);

	return (lngth);
}
