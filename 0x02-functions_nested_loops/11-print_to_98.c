#include "main.h"

/**
 *print_to_98 - printes num up to 98
 *@a: parameter the function takes it is int type
 *Return: typ is void
 */
void print_to_98(int a)
{


	if (a <= 98)
	{
		for (a <=98)
			_putchar("%d", a);
		a++;
	}
	else
	{
		for (i >= 98;)
			_putchar("%d", i);
		a--;
	}
}
