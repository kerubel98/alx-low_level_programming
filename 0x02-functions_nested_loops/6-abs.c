#include "main.h"

/**
 *_abs -returns absulut valu of a 
 *@a: output changes the value of a
 *Return: the systm retuns postive valu of a
 */
int _abs(int a)
{

	if (a >= 0)
	return (a);

	else
	return (-a);
}
